#include "main.h"
/**
 * int _strlen - function to return length of string
 * @s: pointer variable
 * Return: nothing
 */
int _strlen(char *s)
{
int i;
int counter = 0;

for (i = 0 ; s[i]) != ''\0 ; i++)
/* strlen - leturn length of string*/
	count++;

return (count);
}
