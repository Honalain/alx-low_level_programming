#include"main.h"
/**
 *  _abs - function to find absolute value of number
 *  @h: function parameter
 *  Return: -h or h
 */
int _abs(int h)
{
	if (h < 0)
		return (-h);
	else if (h > 0)
	{
	return (h);


	}
	return (0);
}
